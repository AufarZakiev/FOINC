# Module: Result Aggregation

## Purpose
Сборка результатов из завершённых чанков в единый файл, верификация через избыточное вычисление (redundancy), обработка расхождений (tiebreaker), формирование итогового CSV для скачивания.
